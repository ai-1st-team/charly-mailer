# 2026-05-23

## Task 01

Создан каркас CHARLY MAILER: GUI с пятью вкладками, пустые core-модули, `README.md`, `.env.example`, `.gitignore`, `requirements.txt` и стартовая memory-база.

## Task 02

Добавлен proxy manager, storage helpers для TXT, GUI-блок прокси на вкладке `Setup`, загрузка из файла/URL, автообновление URL-списка, проверка всех прокси и JSONL-логи в `logs/`.

## Task 03

Добавлен SMTP manager, загрузка `smtps.txt`, проверка login через `smtplib`, автоопределение SSL/STARTTLS по порту, round-robin живых аккаунтов, session-счётчики, GUI-блок SMTP с карточками аккаунтов на вкладке `Setup` и standalone `test_smtp_login.py`.

## Task 04

Добавлен `SubjectManager`, загрузка `subjects.txt`, случайный выбор темы, рендер плейсхолдеров `{{name}}`, `{{email}}`, `{{senderName}}` и GUI-блок тем на вкладке `Content` с превью и тестовой подстановкой.

## Task 05

Добавлен `BodyManager`, загрузка `bodies.txt` с разделителем `===END===`, рекурсивный парсер вложенного спинтакса, plain/html detection и GUI-блок тел на вкладке `Content` с большим `CTkTextbox` для предпросмотра развёрнутого письма.

## Task 06

Добавлен `LinkManager`, макросы `[[LINK]]`, `[[LINK1]]`, `[[LINK2]]` для тем и тел, режим уникальных ссылок в письме и GUI-блок `Ссылки` на вкладке `Content` со счётчиками загруженных списков.

## Task 07

Добавлен `StatsManager`, вкладка `Stats` с прогресс-баром, глобальными метриками, таблицами SMTP/прокси, дневным логом `logs/YYYY-MM-DD.json` и экспортом JSON/CSV. `Setup` синхронизирует загруженные SMTP/прокси в общий stats manager.

## Task 08

Добавлен общий `AppState`, persistent `QueueManager`, `EmailComposer`, `EmailDeliveryService`, `CampaignController` и полноценная вкладка `Send` с тестовой отправкой, preview письма, start/stop/pause, настройками скорости и восстановлением очереди.

## Task 09

Добавлена вкладка `Campaign` для загрузки CSV базы получателей, preview первых 5 строк, настройка control-инжекта каждые N писем и контрольных адресов через запятую. База и control settings пишутся в общий `AppState`; `QueueManager` вставляет control-письма в очередь. CSV строки с `email`, начинающимся на `#`, пропускаются как комментарии. Проверено: `compileall`, чтение CSV, control-инжект.

## Task 10

Добавлена загрузка `senders.txt` на вкладке `Content`: кнопка, счётчик, preview первых 5 имён и чекбокс `использовать только email без имени`. `SenderNameManager` хранит имена и выбирает случайное имя на каждое письмо. `EmailComposer` использует это имя в `From` и `{{senderName}}`; в email-only режиме From содержит только SMTP email. Проверено: `compileall`, загрузка senders и плейсхолдер `{{senderName}}`.

## Task 11

Добавлены CC/BCC настройки на вкладке `Campaign`: адреса через запятую, проценты применения 0-100. `EmailComposer` случайно добавляет CC/BCC на каждое письмо, `StatsManager` пишет `had_cc`/`had_bcc` в JSON/CSV логи. Добавлен `core/presets.py`, кнопки сохранить/загрузить пресет, автообновление вкладок через `<<PresetLoaded>>`; сохранение блокируется при невалидных Campaign-полях. Добавлены `start.bat` и `start.command`, а `main.py` создаёт `data/`, `data/presets/`, `logs/` при старте. Проверено: `compileall`, compose-test CC/BCC, preset smoke test.
